\ProvidesPackage{style}

\usepackage{fontspec}
\usepackage[]{biblatex} % References
\usepackage{url} % Wrap urls
\usepackage[hidelinks]{hyperref} % Clickable links
\usepackage{siunitx} % Typeset units
\usepackage{graphicx} % Import graphics
\usepackage{tikz} % Drawing
\usepackage{environ} % Sizing tikz
\usepackage{booktabs} % Tables
\usepackage{varioref} % Adapt refs based on distance
\usepackage{vmargin} % Margins
\usepackage{amsmath} % Maths
\usepackage[newfloat]{minted} % Code formatting
\usepackage{wrapfig} % Wrap around figures
\usepackage{caption} % Change caption types
\usepackage{float} % More float styles

\defaultfontfeatures{Ligatures=TeX}

% Set margins
\setmarginsrb{20mm}{5mm}{20mm}{10mm}%
             {12pt}{11mm}{0pt}{11mm}

% Set column separation
\setlength{\columnsep}{20pt}

\graphicspath{{img/}}

\usetikzlibrary{positioning, decorations.markings, shapes} % Add extra tikz libraries

% Create a new environment for scaling tikz pictures
\newsavebox{\measure@tikzpicture}
\NewEnviron{scaletikzpicturetowidth}[1]{%
  \def\tikz@width{#1}%
  \def\tikzscale{1}\begin{lrbox}{\measure@tikzpicture}%
  \BODY
  \end{lrbox}%
  \pgfmathparse{#1/\wd\measure@tikzpicture}%
  \edef\tikzscale{\pgfmathresult}%
  \BODY
}

\sisetup{per-mode=symbol} % (siunitx) Use symbols for 'per' values e.g. m/s
\DeclareSIUnit\feet{ft} % Add the imperial 'feet' unit
\newcommand{\hPa}{\hecto\pascal} % Shorter command for hPa

\DeclareMathOperator{\arctantwo}{arctan2}

% Centre minted code
\RecustomVerbatimEnvironment{Verbatim}{BVerbatim}{}
